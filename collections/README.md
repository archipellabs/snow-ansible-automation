# collections/ — Ansible collection requirements

`requirements.yml` — the collections AAP installs at project sync: `servicenow.itsm` (ITSM + the CMDB
dynamic inventory), `ansible.eda` (rulebook sources), `infra.aap_configuration` (declarative EDA config).

📖 **How the project + collections fit → [docs/03-architecture.md](../docs/03-architecture.md)**.
